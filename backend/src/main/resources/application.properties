spring.application.name=DOAP

# Database Configuration (Use Environment Variables for Cloud DB)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/doap}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Tilak1604}

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Port Configuration (Render uses $PORT)
server.port=${PORT:8081}

# JWT Configuration
jwt.secret=${JWT_SECRET:doap-dev-9c3f7a2b6d4e8f1a0c5b7e9d2a6f8c}
jwt.expiration=3600000

# CORS Configuration
cors.allowed-origins=${FRONTEND_URL:http://localhost:3000}

# Google OAuth Configuration
google.client-id=${GOOGLE_CLIENT_ID:936617371696-paae599e42u4gtbmos1ulqs4j23d52ja.apps.googleusercontent.com}

# AWS Configuration (Get these from Environment Variables)
aws.access.key.id=${AWS_ACCESS_KEY}
aws.secret.access.key=${AWS_SECRET_KEY}
aws.region=${AWS_REGION:ap-south-1}
aws.s3.bucket.temp=${S3_BUCKET_TEMP:doap-temp-uploads}
aws.s3.bucket.approved=${S3_BUCKET_APPROVED:doap-approved-content}

# File Upload Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# ML Service Configuration
ml.service.url=${ML_SERVICE_URL:http://localhost:5000}

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_S9O6BBwHLNvjua}
razorpay.key.secret=${RAZORPAY_KEY_SECRET}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:doap.official16@gmail.com}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
